<input type="email" id="{{ field.name }}" name="{{ field.name }}" value="{{ field.value|default('') }}" class="form-control" placeholder="{{ config.ui_settings.placeholder|default('Enter email address') }}" {% if config.validation.required %} required {% endif %}>
