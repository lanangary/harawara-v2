<input type="text" id="{{ field.name }}" name="{{ field.name }}" value="{{ field.value|default('') }}" class="form-control" placeholder="{{ config.ui_settings.placeholder|default('') }}" {% if config.validation.required %} required {% endif %} {% if config.validation.max_length %} maxlength="{{ config.validation.max_length }}" {% endif %} {% if config.validation.min_length %} minlength="{{ config.validation.min_length }}" {% endif %}>
