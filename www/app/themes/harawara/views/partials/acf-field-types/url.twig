<input type="url" id="{{ field.name }}" name="{{ field.name }}" value="{{ field.value|default('') }}" class="form-control" placeholder="{{ config.ui_settings.placeholder|default('https://example.com') }}" {% if config.validation.required %} required {% endif %}>
