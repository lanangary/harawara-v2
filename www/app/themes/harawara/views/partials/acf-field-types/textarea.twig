<textarea id="{{ field.name }}" name="{{ field.name }}" class="form-control" rows="4" placeholder="{{ config.ui_settings.placeholder|default('') }}" {% if config.validation.required %} required {% endif %} {% if config.validation.max_length %} maxlength="{{ config.validation.max_length }}" {% endif %} {% if config.validation.min_length %} minlength="{{ config.validation.min_length }}" {% endif %}>{{ field.value|default('') }}</textarea>
